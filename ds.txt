<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>上帝之手 · 我给自推当系统？！</title>
    <style>
        :root {
            --bg-color: #0d0d16;
            --panel-bg: #161626;
            --accent-color: #8a2be2;
            --text-color: #e0e0e0;
            --text-muted: #8e8eaf;
            --border-color: #2a2a40;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        /* 左侧数值面板 */
        .sidebar {
            width: 320px;
            background-color: var(--panel-bg);
            border-right: 1px solid var(--border-color);
            display: flex;
            flex-direction: column;
            padding: 20px;
            box-shadow: 2px 0 10px rgba(0,0,0,0.5);
        }

        .game-title {
            font-size: 1.1rem;
            font-weight: bold;
            color: #fff;
            text-align: center;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--accent-color);
            margin-bottom: 20px;
        }

        .section-title {
            font-size: 0.85rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
            margin-top: 15px;
        }

        .stat-group {
            background: rgba(255,255,255,0.03);
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: 12px;
            margin-bottom: 15px;
        }

        .stat-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
            font-size: 0.9rem;
        }
        .stat-row:last-child { margin-bottom: 0; }

        .progress-bar {
            width: 100px;
            height: 6px;
            background: #222;
            border-radius: 3px;
            overflow: hidden;
        }
        .progress-fill {
            height: 100%;
            background: var(--accent-color);
            width: 50%;
            transition: width 0.5s ease;
        }
        .progress-fill.health { background: #ff4757; }
        .progress-fill.jealousy { background: #ffa502; }

        /* 右侧剧情交互区 */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            height: 100%;
            background: radial-gradient(circle at 50% 50%, #111122 0%, #0d0d16 100%);
        }

        .story-container {
            flex: 1;
            overflow-y: auto;
            padding: 40px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .log-entry {
            max-width: 80%;
            line-height: 1.6;
            font-size: 1.05rem;
            animation: fadeIn 0.4s ease forwards;
        }
        .log-entry.system {
            align-self: flex-start;
            color: #b3e5fc;
            background: rgba(3, 169, 244, 0.05);
            padding: 15px;
            border-left: 3px solid #03a9f4;
            border-radius: 0 8px 8px 0;
        }
        .log-entry.story {
            align-self: flex-start;
            color: #fff;
            white-space: pre-line;
        }
        .log-entry.player {
            align-self: flex-end;
            color: #e0a3ff;
            background: rgba(138, 43, 226, 0.1);
            padding: 12px 18px;
            border-radius: 15px 15px 0 15px;
            border: 1px solid rgba(138, 43, 226, 0.3);
        }

        /* 底部控制与选项 */
        .control-panel {
            background-color: var(--panel-bg);
            border-top: 1px solid var(--border-color);
            padding: 20px 40px;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .options-container {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .option-btn {
            background: rgba(255,255,255,0.02);
            border: 1px solid var(--border-color);
            color: var(--text-color);
            padding: 12px 20px;
            border-radius: 6px;
            cursor: pointer;
            text-align: left;
            font-size: 0.95rem;
            transition: all 0.2s ease;
        }
        .option-btn:hover {
            background: var(--accent-color);
            color: #fff;
            border-color: var(--accent-color);
            transform: translateX(5px);
        }

        .custom-input-container {
            display: flex;
            gap: 10px;
        }
        .custom-input {
            flex: 1;
            background: #0d0d16;
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: 12px;
            color: #fff;
            font-size: 0.95rem;
        }
        .custom-input:focus {
            outline: none;
            border-color: var(--accent-color);
        }
        .send-btn {
            background: var(--accent-color);
            color: #fff;
            border: none;
            padding: 0 25px;
            border-radius: 6px;
            cursor: pointer;
            font-weight: bold;
            transition: opacity 0.2s;
        }
        .send-btn:hover { opacity: 0.9; }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="game-title">上帝之手 · 叙事系统</div>
        
        <div class="section-title">当前副本</div>
        <div class="stat-group" style="font-weight: bold; color: #e0a3ff;">
            副本 1：哨向·精神图景崩溃边缘
        </div>

        <div class="section-title">系统与主角状态</div>
        <div class="stat-group">
            <div class="stat-row">
                <span>系统信任值</span>
                <span id="trust-val">35%</span>
            </div>
            <div class="stat-row"><div class="progress-bar"><div id="trust-bar" class="progress-fill" style="width: 35%"></div></div></div>
            
            <div class="stat-row" style="margin-top: 10px;">
                <span>主角健康值</span>
                <span id="health-val">90%</span>
            </div>
            <div class="stat-row"><div class="progress-bar"><div id="health-bar" class="progress-fill health" style="width: 90%"></div></div></div>
        </div>

        <div class="section-title">当前攻略对象</div>
        <div class="stat-group">
            <div class="stat-row" style="font-weight: bold; margin-bottom: 12px;">
                <span>目标人物：</span>
                <span style="color: #fff;">成韩彬 (ENFJ)</span>
            </div>
            <div class="stat-row">
                <span>好感度</span>
                <span id="love-val">50%</span>
            </div>
            <div class="stat-row"><div class="progress-bar"><div id="love-bar" class="progress-fill" style="width: 50%"></div></div></div>
            
            <div class="stat-row" style="margin-top: 10px;">
                <span>妒忌值</span>
                <span id="jealousy-val">15%</span>
            </div>
            <div class="stat-row"><div class="progress-bar"><div id="jealousy-bar" class="progress-fill jealousy" style="width: 15%"></div></div></div>
        </div>
    </div>

    <div class="main-content">
        <div class="story-container" id="story-box">
            <div class="log-entry system">[系统日志] 叙事引擎初始化成功。成功加载世界观：哨兵与向导。</div>
            <div class="log-entry story" id="current-story">
                你在冰冷的纯白房间中醒来。作为“系统”，你能够看到不远处的检测台上，主角正痛苦地揉着太阳穴。
                由于刚刚结束了一场高强度的S级遭遇战，他的向导素已经消耗殆尽，感官过载随时可能让他陷入精神暴走。

                此时，负责安抚他的首席向导成韩彬推门走了进来。他的目光里带着浓浓的担忧，但精神体（白狼）却有些焦躁地在主角身边打转。

                成韩彬看向主角，声音低沉：“别硬撑了，让我建立精神链接。”
                然而，主角因为保有原世界的记忆，对这种过于亲密的“灵魂结合”感到极度不适和抗拒。
            </div>
        </div>

        <div class="control-panel">
            <div class="options-container" id="options-box">
                <button class="option-btn" onclick="makeChoice('发出系统电击提示，警告主角如果不接受安抚，健康值将跌破临界点。')">选项 1：利用系统权限强行警告主角接受安抚</button>
                <button class="option-btn" onclick="makeChoice('偷偷调用系统的“白噪音屏蔽”功能，悄悄帮主角缓解一丝痛苦。')">选项 2：暗中出手，帮主角分担部分感官载荷</button>
            </div>
            <div class="custom-input-container">
                <input type="text" class="custom-input" id="player-input" placeholder="或者，在这里输入身为系统的自定义指令...">
                <button class="send-btn" onclick="sendCustomInput()">发送指令</button>
            </div>
        </div>
    </div>

    <script>
        const storyBox = document.getElementById('story-box');
        const optionsBox = document.getElementById('options-box');

        // 模拟交互逻辑（未来这里要替换为真实的 API 请求）
        function makeChoice(choiceText) {
            // 1. 在画面上添加玩家的选择
            appendLog(choiceText, 'player');
            
            // 2. 模拟 AI 正在思考
            optionsBox.innerHTML = '<div style="color: var(--text-muted)">叙事引擎正在计算下一回合剧情...</div>';
            
            setTimeout(() => {
                // 3. 模拟 AI 返回了新剧本和变化的数值
                const mockStory = "【系统提示：由于你的协助，主角看你的眼神多了一丝动摇。】\n\n成韩彬似乎察觉到了房间内微弱的白噪音波动，他微微一愣，随后立刻上前握住主角的手。由于你的暗中帮助，主角的抵抗没有那么激烈了。成韩彬的好感度上升了，但他看着空气中残留的异样能量，妒忌值隐隐有些浮动。";
                appendLog(mockStory, 'story');
                
                // 4. 模拟刷新左侧面板的数据
                document.getElementById('trust-val').innerText = '45%';
                document.getElementById('trust-bar').style.width = '45%';
                
                document.getElementById('love-val').innerText = '55%';
                document.getElementById('love-bar').style.width = '55%';

                document.getElementById('jealousy-val').innerText = '25%';
                document.getElementById('jealousy-bar').style.width = '25%';

                // 5. 模拟刷新新选项
                optionsBox.innerHTML = `
                    <button class="option-btn" onclick="makeChoice('向成韩彬发布系统伪装任务')">选项 1：假冒主脑发布紧急任务，调开成韩彬</button>
                    <button class="option-btn" onclick="makeChoice('在主角脑海中私聊安慰他')">选项 2：在主角的精神识海中与他对话，建立信任</button>
                `;
            }, 1200);
        }

        function sendCustomInput() {
            const input = document.getElementById('player-input');
            if(input.value.trim() !== "") {
                makeChoice("【自定义指令】" + input.value);
                input.value = "";
            }
        }

        function appendLog(text, type) {
            const div = document.createElement('div');
            div.className = `log-entry ${type}`;
            div.innerText = text;
            storyBox.appendChild(div);
            storyBox.scrollTop = storyBox.scrollHeight;
        }
    </script>
</body>
</html>